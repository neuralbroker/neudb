# neuDB — install options
#
# Core (zero dependencies):
#   pip install -e .
#
# With HTTP API:
#   pip install -e ".[api]"
#
# With LLM agent + embeddings:
#   pip install -e ".[agent]"
#
# Development:
#   pip install -e ".[test,api,agent]"